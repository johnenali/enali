= 特性features =
# 任意对象的有序集合
# 通过偏移存取(支持基于偏移的操作如索引和分片）
# 不可变序列
# 固定长度，异构，任意嵌套
# 对象引用数组（存储引用而非对象实体）
= 操作 =
# () T=(0,)','必须 T=(0,'NI',1.2,3) T=0,'NI',1.2,3同前,T=('abc',('bcd','def')) T=tuple('spam') T[i] T[i][j] T[i:j] len(T) 
# T1+T2 T*3
# for x in T: print(x)
# 'spam' in T
# [x**2 for x in T]
# T.index('Ni')
# T.count('Ni')
# 排序：转为列表->排序.sort->转为元组；内置函数sorted
= 注意 =
# 仅当元组作为常量传给函数调用时，圆括号才是必须的。
# 注意元组的不可变性只适用于元组本身顶层而并非其内容，若元组嵌套列表，则列表元素内容可变。（单层深度的不可变性）
# 元组的不可变性提供了某种完整性，可确保元组在程序中不会被另一个引用修改。
