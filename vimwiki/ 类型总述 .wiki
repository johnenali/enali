# 无条件值的分片以及字典copy方法只能做顶层复制，不能复制嵌套。
# 模块copy:copy.copy(), copy.deepcopy()
# 比较：数字通过相对大小比较；字符串按照字典顺序字符比较；列表元组从左到右每部分都比较。
# 字典比较sorted(D1.items()) > sorted(D2.items())
# 真和假的概念是python中每个对象的固有属性；每个对象不是真就是假。数字：非零则真；对象：非空则真；None是一个对象，占内存，由python给定内置名称，总假。
# 重复，合并，分片只复制操作数对象的顶层。
    - L = [4, 5, 6]
    - X = L * 4 -> [4, 5, 6, 4, 5, 6, 4, 5, 6, 4, 5, 6]
    - Y = [L] * 4 -> [[4, 5, 6], [4, 5, 6], [4, 5, 6], [4, 5, 6]]
# 循环引用：复合对象包含指向自身的引用，在python打印为[...]

